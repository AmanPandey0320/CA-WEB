import { AppBar, Toolbar } from "@mui/material"

const Navbar = (props:any) => {
    return(
        <AppBar>
            <Toolbar>
                NAV
            </Toolbar>
        </AppBar>
    )
}

export default Navbar;